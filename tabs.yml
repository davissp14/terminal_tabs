terminal: iTerm       # Specify terminal type.  Currently only compatable with iTerm.

# Autoload tasks by placing the name and (path / cmd ) of the task you want included.
tabs:
#  tail: "cd /Users/shaun/Programming/tail && npm start"
#  api: "cd /Users/shaun/Programming//api && bundle exec rackup config.ru"
#  vagrant: "cd /Users/shaun/Programming/vagrant && vagrant up"
